// Strings

//backtick

// we can use the single quote double quote and backtick

// we cannot mutate it

// i can only modify , i cannot change it

// for using the single quotes inside the console.log
// we need to use single quote and double quote

//    \' 

// we can concatenate the string using the + sign

